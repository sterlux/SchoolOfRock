namespace SoR{
    // El enum es para cuando tenemos algo ya definido 
    // Asi tenemos una mejor estructura en el codigo
    public enum Level{
        Beginner = 0, // por default tienen valores numericos
        Medium, // no pasa nada si no ponemos el valor
        Pro = 2
    }
}